export function addDecimals(amount: string, decimals: number): bigint {
  // TODO: implement so it works with JavaScript bigint
  return 1n;
}

export function removeDecimals(
  amount: bigint | string,
  decimals: number
): string {
  // TODO: implement so it works with JavaScript bigint
  return '';
}
